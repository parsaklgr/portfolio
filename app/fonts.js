import { DynaPuff, Merienda, Silkscreen, Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const dynaPuff = DynaPuff({
  subsets: ["latin"],
  display: "swap",
});

export const merienda = Merienda({
  subsets: ["latin"],
  display: "swap",
});
export const silkscreen = Silkscreen({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});
